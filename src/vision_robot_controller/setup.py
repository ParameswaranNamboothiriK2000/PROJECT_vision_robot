from setuptools import setup

package_name = 'vision_robot_controller'

setup(
    name=package_name,
    version='1.0.0',
    packages=[package_name],
    data_files=[],
    install_requires=['setuptools'],
    zip_safe=True,
    maintainer='user',
    maintainer_email='user@example.com',
    description='Vision Robot Controller',
    license='Apache-2.0',
    entry_points={},
)
